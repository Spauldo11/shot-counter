import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shotcounter.app',
  appName: 'Shot Counter for Net Sports',
  webDir: 'build'
};

export default config;
